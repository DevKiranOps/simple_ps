# Starter pipeline
- task: PowerShell@2
  inputs:
    filePath: 'simple_install.ps1'
    failOnStderr: true
    workingDirectory: 'c:\users\adminuser'
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
